import { CartQuantityEnum } from "@/Constants/AppConstant"

export function handleCart(type: CartQuantityEnum, productId: string, price: number, navigateTo?: string) {
	// API call 
	// dispatch and store data - persist

}